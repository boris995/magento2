<?php
/**
 * Product card template for JerseyStore PLP.
 *
 * @var \Magento\Framework\View\Element\Template $block
 * @var \Magento\Framework\Escaper $escaper
 */

use Magento\Framework\App\Action\Action;

/** @var \Magento\Catalog\Model\Product $_product */
$_product = $block->getData('product');
/** @var \Magento\Catalog\Block\Product\ListProduct $_listBlock */
$_listBlock = $block->getData('list_block');
$_imageDisplayArea = $block->getData('image_display_area') ?: 'category_page_grid';
$_templateType = $block->getData('template_type') ?: \Magento\Catalog\Block\Product\ReviewRendererInterface::SHORT_VIEW;
$_outputHelper = $_listBlock->getData('outputHelper');
$_productImage = $_listBlock->getImage($_product, $_imageDisplayArea);
$_productName = $_listBlock->stripTags($_product->getName(), null, true);
$_viewModel = $block->getData('jersey_category_products');
$_priceHelper = $this->helper(\Magento\Framework\Pricing\Helper\Data::class);
$_stockData = $_viewModel ? $_viewModel->getStockData($_product) : ['is_in_stock' => null, 'qty' => null];
$_productUrl = $_viewModel ? $_viewModel->getProductUrl($_product) : $_product->getProductUrl();
?>

<article
    class="group flex h-full flex-col overflow-hidden border border-neutral-200 bg-white transition duration-300 hover:-translate-y-1 hover:border-[#d60000] hover:shadow-2xl hover:shadow-neutral-950/10"
    id="product-item-info_<?= (int) $_product->getId() ?>"
>
    <a
        href="<?= $escaper->escapeUrl($_productUrl) ?>"
        class="relative block bg-gradient-to-br from-neutral-100 via-white to-neutral-200 p-4"
        tabindex="-1"
    >
        <span class="absolute left-4 top-4 z-10 bg-[#d60000] px-3 py-1 text-[11px] font-black uppercase tracking-[0.12em] text-white">
            <?= $escaper->escapeHtml($_stockData['is_in_stock'] === false ? __('Nema na stanju') : __('Dres')) ?>
        </span>
        <div class="aspect-[4/5] overflow-hidden bg-white">
            <div class="h-full w-full transition duration-500 group-hover:scale-105">
                <?= $_productImage->toHtml() ?>
            </div>
        </div>
    </a>

    <div class="flex flex-1 flex-col p-5">
        <div class="flex-1">
            <h2 class="text-lg font-black leading-snug text-neutral-950">
                <a
                    href="<?= $escaper->escapeUrl($_productUrl) ?>"
                    class="!text-neutral-950 !no-underline transition hover:!text-[#d60000]"
                    title="<?= $escaper->escapeHtmlAttr($_productName) ?>"
                >
                    <?= /* @noEscape */ $_outputHelper->productAttribute($_product, $_product->getName(), 'name') ?>
                </a>
            </h2>

            <div class="mt-3 text-sm text-neutral-500">
                <?= $_listBlock->getReviewsSummaryHtml($_product, $_templateType) ?>
            </div>

            <div class="mt-4 text-2xl font-black text-neutral-950">
                <?= /* @noEscape */ $_priceHelper->currency($_product->getFinalPrice(), true, false) ?>
            </div>

            <?= $_listBlock->getProductDetailsHtml($_product) ?>

            <dl class="mt-4 grid grid-cols-2 border border-neutral-200 text-sm">
                <div class="border-r border-neutral-200 bg-neutral-50 p-3">
                    <dt class="font-black text-neutral-500"><?= $escaper->escapeHtml(__('Stanje')) ?></dt>
                    <dd class="mt-1 font-bold text-neutral-950">
                        <?php if ($_stockData['is_in_stock'] === null): ?>
                            <?= $escaper->escapeHtml(__('Nepoznato')) ?>
                        <?php else: ?>
                            <?= $escaper->escapeHtml($_stockData['is_in_stock'] ? __('Dostupno') : __('Nema')) ?>
                        <?php endif; ?>
                    </dd>
                </div>
                <div class="bg-neutral-50 p-3">
                    <dt class="font-black text-neutral-500"><?= $escaper->escapeHtml(__('Tip')) ?></dt>
                    <dd class="mt-1 font-bold text-neutral-950">
                        <?= $escaper->escapeHtml(__('Original')) ?>
                    </dd>
                </div>
            </dl>
        </div>

        <div class="mt-5 flex flex-col gap-3">
            <?php if ($_product->isSaleable()): ?>
                <?php $_postParams = $_listBlock->getAddToCartPostParams($_product); ?>
                <form
                    data-role="tocart-form"
                    data-product-sku="<?= $escaper->escapeHtmlAttr($_product->getSku()) ?>"
                    action="<?= $escaper->escapeUrl($_postParams['action']) ?>"
                    data-mage-init='{"catalogAddToCart": {}}'
                    method="post"
                >
                    <input type="hidden" name="product" value="<?= (int) $_postParams['data']['product'] ?>" />
                    <input
                        type="hidden"
                        name="<?= /* @noEscape */ Action::PARAM_NAME_URL_ENCODED ?>"
                        value="<?= /* @noEscape */ $_postParams['data'][Action::PARAM_NAME_URL_ENCODED] ?>"
                    />
                    <?= $_listBlock->getBlockHtml('formkey') ?>
                    <button
                        type="submit"
                        title="<?= $escaper->escapeHtmlAttr(__('Add to Cart')) ?>"
                        class="w-full bg-[#d60000] px-5 py-3 text-sm font-black uppercase tracking-[0.1em] text-white transition hover:bg-neutral-950"
                    >
                        <span><?= $escaper->escapeHtml(__('Dodaj u korpu')) ?></span>
                    </button>
                </form>
            <?php else: ?>
                <div class="border border-neutral-200 px-5 py-3 text-center text-sm font-black uppercase tracking-[0.1em] text-neutral-500">
                    <?= $escaper->escapeHtml($_product->isAvailable() ? __('Dostupno') : __('Nema na stanju')) ?>
                </div>
            <?php endif; ?>

            <a
                href="<?= $escaper->escapeUrl($_productUrl) ?>"
                class="text-center text-sm font-black !text-neutral-500 !no-underline transition hover:!text-neutral-950"
            >
                <?= $escaper->escapeHtml(__('Detalji proizvoda')) ?>
            </a>
        </div>
    </div>
</article>
