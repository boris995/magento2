<?php
/**
 * Custom JerseyStore PDP template.
 *
 * @var \Magento\Catalog\Block\Product\View $block
 * @var \Magento\Framework\Escaper $escaper
 */

$_product = $block->getProduct();
$_outputHelper = $this->helper(\Magento\Catalog\Helper\Output::class);
$_imageHelper = $this->helper(\Magento\Catalog\Helper\Image::class);
$_priceHelper = $this->helper(\Magento\Framework\Pricing\Helper\Data::class);
$_productName = $_product ? (string) $_product->getName() : '';
$_shortDescription = $_product ? (string) $_product->getShortDescription() : '';
$_description = $_product ? trim((string) $_product->getData('description')) : '';
$_descriptionHtml = $_description !== ''
    ? trim((string) $_outputHelper->productAttribute($_product, $_description, 'description'))
    : '';
$_finalPrice = $_product ? (float) $_product->getFinalPrice() : 0;
$_regularPrice = $_product ? (float) $_product->getPrice() : 0;
$_hasDiscount = $_regularPrice > $_finalPrice && $_finalPrice > 0;
$_discountPercent = $_hasDiscount ? (int) round((($_regularPrice - $_finalPrice) / $_regularPrice) * 100) : 0;
$_galleryImages = $_product ? $_product->getMediaGalleryImages() : null;
$_heroImage = $_product
    ? $_imageHelper->init($_product, 'product_page_image_large')->keepFrame(false)->getUrl()
    : '';
$_thumbnailImage = $_product
    ? $_imageHelper->init($_product, 'product_page_image_small')->keepFrame(false)->getUrl()
    : '';
$_galleryItems = [];

if ($_galleryImages && $_galleryImages->getSize()) {
    foreach ($_galleryImages as $_image) {
        if (count($_galleryItems) >= 4) {
            break;
        }

        $_galleryItems[] = [
            'url' => (string) $_image->getUrl(),
            'label' => (string) ($_image->getLabel() ?: $_productName),
        ];
    }
}

if (!count($_galleryItems) && $_heroImage) {
    $_galleryItems[] = [
        'url' => $_heroImage,
        'label' => $_productName,
    ];
}
?>

<?php if ($_product): ?>
<main class="bg-white text-[#0b0b0b]">
    <section class="container-shop py-6 sm:py-9 lg:py-12">
        <div class="grid gap-10 lg:grid-cols-[minmax(0,1.06fr)_minmax(430px,0.94fr)] lg:items-start xl:gap-16">
            <div class="grid gap-4 sm:grid-cols-[92px_minmax(0,1fr)] lg:sticky lg:top-24">
                <div class="order-2 flex gap-2 overflow-x-auto pb-1 sm:order-1 sm:flex-col sm:overflow-visible sm:pb-0">
                    <?php foreach ($_galleryItems as $_index => $_item): ?>
                        <button
                            type="button"
                            class="<?= $_index === 0 ? 'border-[#1d4ed8] ring-4 ring-blue-100' : 'border-slate-200' ?> js-pdp-thumb grid aspect-square min-w-20 place-items-center overflow-hidden rounded-md border-2 bg-white p-2 shadow-sm transition hover:border-[#1d4ed8] hover:ring-4 hover:ring-blue-50 sm:min-w-0"
                            data-full-image="<?= $escaper->escapeUrl($_item['url']) ?>"
                            aria-label="<?= $escaper->escapeHtmlAttr(__('Prikaži sliku proizvoda')) ?>"
                        >
                            <img
                                class="h-full w-full object-contain"
                                src="<?= $escaper->escapeUrl($_item['url']) ?>"
                                alt="<?= $escaper->escapeHtmlAttr($_item['label']) ?>"
                                loading="<?= $_index === 0 ? 'eager' : 'lazy' ?>"
                            />
                        </button>
                    <?php endforeach; ?>

                    <button type="button" class="grid aspect-square min-w-20 place-items-center rounded-md border border-slate-200 bg-white text-center text-[11px] font-black uppercase text-slate-900 shadow-sm transition hover:border-[#1d4ed8] hover:text-[#1d4ed8] sm:min-w-0">
                        <svg class="mx-auto mb-1 h-7 w-7" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                            <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.8"/>
                            <path d="m10 8 6 4-6 4V8Z" fill="currentColor"/>
                        </svg>
                        <?= $escaper->escapeHtml(__('Video')) ?>
                    </button>
                </div>

                <div class="relative order-1 overflow-hidden rounded-lg bg-white shadow-[0_18px_60px_rgba(15,23,42,0.08)] ring-1 ring-slate-200 sm:order-2">
                    <?php if ($_hasDiscount): ?>
                        <div class="absolute right-4 top-4 z-10 rounded bg-[#1d4ed8] px-3 py-2 text-lg font-black text-white shadow-lg shadow-blue-900/20">
                            -<?= (int) $_discountPercent ?>%
                        </div>
                    <?php endif; ?>
                    <div class="grid aspect-[4/5] min-h-[420px] place-items-center bg-gradient-to-br from-white via-slate-50 to-blue-50/70 p-5 sm:min-h-[580px] sm:p-8 lg:min-h-[650px]">
                        <img
                            id="jersey-pdp-main-image"
                            class="h-full max-h-[640px] w-full object-contain"
                            src="<?= $escaper->escapeUrl($_heroImage) ?>"
                            alt="<?= $escaper->escapeHtmlAttr($_productName) ?>"
                        />
                    </div>
                </div>
            </div>

            <div class="rounded-lg bg-white p-0 lg:border lg:border-slate-200 lg:p-7 lg:shadow-[0_18px_60px_rgba(15,23,42,0.06)]">
                <nav class="mb-5 flex w-full items-center gap-2 overflow-x-auto whitespace-nowrap rounded-md bg-blue-50 px-3 py-2 text-[11px] font-black uppercase tracking-[0.1em] text-slate-500 ring-1 ring-blue-100">
                    <a class="!text-slate-500 !no-underline transition hover:!text-[#1d4ed8]" href="<?= $escaper->escapeUrl($block->getUrl('')) ?>">
                        <?= $escaper->escapeHtml(__('Početna')) ?>
                    </a>
                    <span class="text-blue-300" aria-hidden="true">/</span>
                    <a class="!text-slate-500 !no-underline transition hover:!text-[#1d4ed8]" href="<?= $escaper->escapeUrl($block->getUrl('category-page')) ?>">
                        <?= $escaper->escapeHtml(__('Retro dresovi')) ?>
                    </a>
                    <span class="text-blue-300" aria-hidden="true">/</span>
                    <strong class="truncate font-black text-slate-950"><?= $escaper->escapeHtml($_productName) ?></strong>
                </nav>

                <div class="mb-3 inline-flex items-center rounded bg-blue-50 px-3 py-1.5 text-[11px] font-black uppercase tracking-[0.16em] text-[#1d4ed8] ring-1 ring-blue-100">
                    <?= $escaper->escapeHtml(__('Premium retro jersey')) ?>
                </div>

                <h1 class="max-w-[680px] text-3xl font-black leading-tight tracking-[-0.01em] text-slate-950 sm:text-4xl lg:text-[44px]">
                    <?= /* @noEscape */ $_outputHelper->productAttribute($_product, $_productName, 'name') ?>
                </h1>

                <div class="mt-5 flex flex-wrap items-end gap-4 border-b border-slate-200 pb-5">
                    <strong class="text-4xl font-black leading-none text-[#1d4ed8]">
                        <?= /* @noEscape */ $_priceHelper->currency($_finalPrice, true, false) ?>
                    </strong>
                    <?php if ($_hasDiscount): ?>
                        <span class="pb-1 text-lg font-bold text-slate-400 line-through">
                            <?= /* @noEscape */ $_priceHelper->currency($_regularPrice, true, false) ?>
                        </span>
                        <span class="mb-1 rounded bg-blue-50 px-3 py-2 text-xs font-black uppercase text-[#1d4ed8] ring-1 ring-blue-100">
                            <?= $escaper->escapeHtml(__('Ušteda')) ?> <?= /* @noEscape */ $_priceHelper->currency($_regularPrice - $_finalPrice, true, false) ?>
                        </span>
                    <?php endif; ?>
                </div>

                <ul class="mt-5 grid gap-3 text-[15px] font-semibold leading-6 text-slate-800">
                    <?php
                    $sellingPoints = [
                        __('100% prozračni poliester za udobnost tokom cijelog dana.'),
                        __('Retro detalji inspirisani originalnim fudbalskim dresovima.'),
                        __('Regular fit kroj koji dobro stoji uz jeans, trenerku ili šorc.'),
                    ];
                    ?>
                    <?php foreach ($sellingPoints as $point): ?>
                        <li class="flex gap-3">
                            <span class="mt-1.5 grid h-5 w-5 shrink-0 place-items-center rounded-full bg-blue-50 text-[#1d4ed8] ring-1 ring-blue-100" aria-hidden="true">
                                <svg class="h-3.5 w-3.5" viewBox="0 0 20 20" fill="none">
                                    <path d="m5 10 3 3 7-7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                </svg>
                            </span>
                            <span><?= $escaper->escapeHtml($point) ?></span>
                        </li>
                    <?php endforeach; ?>
                </ul>

                <div class="mt-5 max-w-[640px] rounded-md bg-slate-50 p-4 text-[15px] leading-7 text-slate-700 ring-1 ring-slate-100">
                    <?php if ($_shortDescription): ?>
                        <?= /* @noEscape */ $_outputHelper->productAttribute($_product, $_shortDescription, 'short_description') ?>
                    <?php else: ?>
                        <?= $escaper->escapeHtml(__('Zvanični fudbalski dres sa laganim materijalom, modernim krojem i udobnošću za svakodnevno nošenje.')) ?>
                    <?php endif; ?>
                </div>

                <form
                    class="mt-7 border-t border-slate-200 pt-6"
                    data-product-sku="<?= $escaper->escapeHtmlAttr($_product->getSku()) ?>"
                    action="<?= $escaper->escapeUrl($block->getSubmitUrl($_product)) ?>"
                    method="post"
                    id="product_addtocart_form"
                    <?php if ($_product->getOptions()): ?> enctype="multipart/form-data"<?php endif; ?>
                >
                    <input type="hidden" name="product" value="<?= (int) $_product->getId() ?>" />
                    <input type="hidden" name="selected_configurable_option" value="" />
                    <input type="hidden" name="related_product" id="related-products-field" value="" />
                    <input type="hidden" name="item" value="<?= (int) $block->getRequest()->getParam('id') ?>" />
                    <?= $block->getBlockHtml('formkey') ?>

                    <div class="flex items-center justify-between gap-4">
                        <span class="text-sm font-black text-slate-950"><?= $escaper->escapeHtml(__('Veličina')) ?></span>
                        <a href="#" class="!text-sm !font-bold !text-[#1d4ed8] !no-underline underline-offset-4 hover:!text-blue-700"><?= $escaper->escapeHtml(__('Vodič za veličine')) ?></a>
                    </div>
                    <div class="mt-3 grid grid-cols-5 gap-2">
                        <?php foreach (['S', 'M', 'L', 'XL', 'XXL'] as $size): ?>
                            <label class="cursor-pointer">
                                <input class="peer sr-only" type="radio" name="jersey_size" value="<?= $escaper->escapeHtmlAttr($size) ?>" <?= $size === 'M' ? 'checked' : '' ?> />
                                <span class="grid h-12 place-items-center rounded-md border border-slate-300 bg-white text-sm font-black text-slate-950 transition peer-checked:border-[#1d4ed8] peer-checked:bg-[#1d4ed8] peer-checked:text-white peer-checked:shadow-lg peer-checked:shadow-blue-900/10 hover:border-[#1d4ed8]">
                                    <?= $escaper->escapeHtml($size) ?>
                                </span>
                            </label>
                        <?php endforeach; ?>
                    </div>

                    <label class="mt-5 block text-sm font-black text-slate-950" for="qty">
                        <?= $escaper->escapeHtml(__('Količina')) ?>
                    </label>
                    <div class="mt-2 grid items-stretch gap-3 sm:grid-cols-[132px_minmax(0,1fr)]">
                        <div class="grid h-14 grid-cols-3 overflow-hidden rounded-md border border-slate-300 bg-white">
                            <button type="button" class="js-qty-minus grid h-full place-items-center text-xl font-black leading-none text-slate-950 transition hover:bg-blue-50 hover:text-[#1d4ed8]" aria-label="<?= $escaper->escapeHtmlAttr(__('Smanji količinu')) ?>">−</button>
                            <input
                                type="number"
                                name="qty"
                                id="qty"
                                min="1"
                                value="<?= (float) $block->getProductDefaultQty() ?: 1 ?>"
                                aria-label="<?= $escaper->escapeHtmlAttr(__('Količina')) ?>"
                                class="grid h-full w-full place-items-center border-x border-slate-300 p-0 text-center text-base font-black leading-none text-slate-950 outline-none [appearance:textfield] focus:bg-blue-50 [&::-webkit-inner-spin-button]:appearance-none [&::-webkit-outer-spin-button]:appearance-none"
                                data-validate="<?= $escaper->escapeHtmlAttr(json_encode($block->getQuantityValidators())) ?>"
                            />
                            <button type="button" class="js-qty-plus grid h-full place-items-center text-xl font-black leading-none text-slate-950 transition hover:bg-blue-50 hover:text-[#1d4ed8]" aria-label="<?= $escaper->escapeHtmlAttr(__('Povećaj količinu')) ?>">+</button>
                        </div>

                        <?php if ($_product->isSaleable()): ?>
                            <button
                                type="submit"
                                class="grid h-14 min-w-0 grid-cols-[24px_minmax(0,1fr)_24px] items-center rounded-md bg-[#1d4ed8] px-5 text-sm font-black uppercase leading-none tracking-[0.18em] text-white shadow-xl shadow-blue-900/20 transition hover:-translate-y-0.5 hover:bg-blue-700 sm:text-base"
                                id="product-addtocart-button"
                                aria-label="<?= $escaper->escapeHtmlAttr(__('Dodaj u korpu')) ?>"
                            >
                                <svg class="h-5 w-5 justify-self-start" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                                    <path d="M4 5h2l2.2 10.2a2 2 0 0 0 2 1.6h6.6a2 2 0 0 0 2-1.5L20 9H7M10 21a1 1 0 1 0 0-2 1 1 0 0 0 0 2Zm8 0a1 1 0 1 0 0-2 1 1 0 0 0 0 2Z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                                </svg>
                                <span class="justify-self-center text-center"><?= $escaper->escapeHtml(__('Dodaj u korpu')) ?></span>
                                <span aria-hidden="true"></span>
                            </button>
                        <?php else: ?>
                            <div class="grid h-14 place-items-center rounded-md bg-slate-200 px-3 text-xs font-black uppercase text-slate-500 sm:px-6 sm:text-sm">
                                <?= $escaper->escapeHtml(__('Nije dostupno')) ?>
                            </div>
                        <?php endif; ?>
                    </div>
                </form>

                <div class="mt-5 grid gap-3 border-y border-slate-100 py-5 sm:grid-cols-3">
                    <?php
                    $serviceItems = [
                        [__('Sigurno plaćanje'), __('kartica ili pouzeće')],
                        [__('Brza dostava'), __('1-3 radna dana')],
                        [__('14 dana povrat'), __('bez komplikacija')],
                    ];
                    ?>
                    <?php foreach ($serviceItems as $serviceItem): ?>
                        <div class="flex items-center justify-center gap-3 rounded-md bg-blue-50 px-3 py-3 ring-1 ring-blue-100">
                            <svg class="h-6 w-6 shrink-0 text-[#1d4ed8]" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                                <path d="M4 7h16v10H4V7Zm3 13h10M8 11h8" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                            </svg>
                            <div>
                                <div class="text-[13px] font-black leading-4 text-slate-950"><?= $escaper->escapeHtml($serviceItem[0]) ?></div>
                                <div class="mt-0.5 text-[11px] leading-4 text-slate-500"><?= $escaper->escapeHtml($serviceItem[1]) ?></div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>
    </section>

    <section class="container-shop border-t border-slate-200 pb-10 pt-8 sm:pb-14 sm:pt-10">
        <div class="mb-6">
            <p class="text-xs font-black uppercase tracking-[0.28em] text-[#1d4ed8]"><?= $escaper->escapeHtml(__('Detalji proizvoda')) ?></p>
            <h2 class="mt-2 text-2xl font-black text-slate-950 sm:text-3xl"><?= $escaper->escapeHtml(__('Sve što trebaš znati prije kupovine')) ?></h2>
        </div>

        <div class="grid gap-8 py-7 lg:grid-cols-[minmax(0,1fr)_420px] lg:items-start">
            <div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-[0_18px_60px_rgba(15,23,42,0.06)]">
                <div class="border-b border-slate-200 bg-slate-50/80 px-3 pt-3">
                    <div class="js-pdp-tabs flex gap-2 overflow-x-auto text-sm font-black uppercase tracking-[0.08em] text-slate-500" role="tablist" aria-label="<?= $escaper->escapeHtmlAttr(__('Detalji proizvoda')) ?>">
                        <button type="button" class="js-pdp-tab rounded-t-md border border-b-0 border-[#1d4ed8] bg-[#1d4ed8] px-5 py-3 text-white shadow-sm" data-tab-target="opis" aria-controls="opis" aria-selected="true" role="tab"><?= $escaper->escapeHtml(__('Opis')) ?></button>
                        <button type="button" class="js-pdp-tab rounded-t-md border border-b-0 border-transparent px-5 py-3 text-slate-500 transition hover:bg-white hover:text-[#1d4ed8]" data-tab-target="detalji" aria-controls="detalji" aria-selected="false" role="tab"><?= $escaper->escapeHtml(__('Detalji')) ?></button>
                        <button type="button" class="js-pdp-tab rounded-t-md border border-b-0 border-transparent px-5 py-3 text-slate-500 transition hover:bg-white hover:text-[#1d4ed8]" data-tab-target="dostava" aria-controls="dostava" aria-selected="false" role="tab"><?= $escaper->escapeHtml(__('Dostava')) ?></button>
                        <button type="button" class="js-pdp-tab rounded-t-md border border-b-0 border-transparent px-5 py-3 text-slate-500 transition hover:bg-white hover:text-[#1d4ed8]" data-tab-target="povrat" aria-controls="povrat" aria-selected="false" role="tab"><?= $escaper->escapeHtml(__('Povrat')) ?></button>
                        <button type="button" class="js-pdp-tab rounded-t-md border border-b-0 border-transparent px-5 py-3 text-slate-500 transition hover:bg-white hover:text-[#1d4ed8]" data-tab-target="reviews" aria-controls="reviews" aria-selected="false" role="tab"><?= $escaper->escapeHtml(__('Recenzije')) ?></button>
                    </div>
                </div>

                <div class="min-h-[320px] p-6 sm:p-8">
                <article id="opis" class="js-pdp-tab-panel">
                    <h3 class="text-lg font-black uppercase tracking-[0.08em] text-[#1d4ed8]"><?= $escaper->escapeHtml(__('Opis')) ?></h3>
                    <div class="mt-4 text-[15px] leading-7 text-slate-700">
                        <?php if ($_descriptionHtml !== ''): ?>
                            <?= /* @noEscape */ $_descriptionHtml ?>
                        <?php else: ?>
                            <p><?= $escaper->escapeHtml(__('Retro fudbalski dres za svaki dan, trening ili kolekcionarski outfit. Lagana tkanina, čist kroj i detalji koji podsjećaju na zlatne fudbalske ere.')) ?></p>
                        <?php endif; ?>
                    </div>
                </article>

                <article id="detalji" class="js-pdp-tab-panel hidden">
                    <h3 class="text-lg font-black uppercase tracking-[0.08em] text-[#1d4ed8]"><?= $escaper->escapeHtml(__('Detalji')) ?></h3>
                    <dl class="mt-5 grid gap-3 text-sm sm:grid-cols-2">
                        <div class="flex justify-between gap-4 rounded-md bg-slate-50 px-4 py-3">
                            <dt class="font-bold text-slate-500"><?= $escaper->escapeHtml(__('Materijal')) ?></dt>
                            <dd class="font-black text-slate-950"><?= $escaper->escapeHtml(__('Poliester')) ?></dd>
                        </div>
                        <div class="flex justify-between gap-4 rounded-md bg-slate-50 px-4 py-3">
                            <dt class="font-bold text-slate-500"><?= $escaper->escapeHtml(__('Kroj')) ?></dt>
                            <dd class="font-black text-slate-950"><?= $escaper->escapeHtml(__('Regular fit')) ?></dd>
                        </div>
                        <div class="flex justify-between gap-4 rounded-md bg-slate-50 px-4 py-3">
                            <dt class="font-bold text-slate-500"><?= $escaper->escapeHtml(__('SKU')) ?></dt>
                            <dd class="font-black text-slate-950"><?= $escaper->escapeHtml($_product->getSku()) ?></dd>
                        </div>
                        <div class="flex justify-between gap-4 rounded-md bg-slate-50 px-4 py-3">
                            <dt class="font-bold text-slate-500"><?= $escaper->escapeHtml(__('Održavanje')) ?></dt>
                            <dd class="font-black text-slate-950"><?= $escaper->escapeHtml(__('Pranje na 30°C')) ?></dd>
                        </div>
                    </dl>
                </article>

                <article id="dostava" class="js-pdp-tab-panel hidden">
                    <h3 class="text-lg font-black uppercase tracking-[0.08em] text-[#1d4ed8]"><?= $escaper->escapeHtml(__('Dostava')) ?></h3>
                    <p class="mt-4 text-[15px] leading-7 text-slate-700"><?= $escaper->escapeHtml(__('Narudžbe se pakuju brzo i šalju pouzdanom dostavom. Nakon slanja dobijaš informacije za praćenje pošiljke.')) ?></p>
                </article>

                <article id="povrat" class="js-pdp-tab-panel hidden">
                    <h3 class="text-lg font-black uppercase tracking-[0.08em] text-[#1d4ed8]"><?= $escaper->escapeHtml(__('Povrat i zamjena')) ?></h3>
                    <p class="mt-4 text-[15px] leading-7 text-slate-700"><?= $escaper->escapeHtml(__('Ako veličina ne odgovara, proizvod možeš vratiti ili zamijeniti u roku od 14 dana, pod uslovom da nije nošen ili oštećen.')) ?></p>
                </article>

                <article id="reviews" class="js-pdp-tab-panel hidden">
                    <h3 class="text-lg font-black uppercase tracking-[0.08em] text-[#1d4ed8]"><?= $escaper->escapeHtml(__('Recenzije')) ?></h3>
                    <div class="mt-4 text-[15px] leading-7 text-slate-700">
                        <p><?= $escaper->escapeHtml(__('Recenzije kupaca će biti prikazane ovdje kada budu dostupne.')) ?></p>
                    </div>
                </article>
                </div>
            </div>

            <div class="grid rounded-lg bg-white shadow-[0_18px_60px_rgba(15,23,42,0.05)] ring-1 ring-slate-200 sm:grid-cols-4">
                <?php
                $footerBenefits = [
                    [__('Zvanični dresovi'), __('100% original'), 'M7 4h10l1.5 4-3 2v10H8.5V10l-3-2L7 4Zm3 0c.5 1.2 1.2 1.8 2 1.8S13.5 5.2 14 4'],
                    [__('Više od 5000+'), __('zadovoljnih kupaca'), 'M12 3 20 7v6c0 4-3.4 6.8-8 8-4.6-1.2-8-4-8-8V7l8-4Zm0 5.2 1.1 2.2 2.4.3-1.7 1.7.4 2.4-2.2-1.1-2.2 1.1.4-2.4-1.7-1.7 2.4-.3L12 8.2Z'],
                    [__('Podrška 24/7'), __('Tu smo za vas'), 'M4 13v-1a8 8 0 0 1 16 0v1M4 13h3v5H5a1 1 0 0 1-1-1v-4Zm13 0h3v4a1 1 0 0 1-1 1h-2v-5Zm0 5c0 2-1.5 3-5 3'],
                    [__('Dostava širom'), __('sveta'), 'M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Zm-8-9h16M12 3c2.2 2.4 3.2 5.4 3.2 9S14.2 18.6 12 21c-2.2-2.4-3.2-5.4-3.2-9S9.8 5.4 12 3Z'],
                ];
                ?>
                <?php foreach ($footerBenefits as $footerBenefit): ?>
                    <div class="border-b border-slate-200 p-5 text-center last:border-b-0 sm:border-b-0 sm:border-r sm:last:border-r-0">
                        <svg class="mx-auto mb-3 h-11 w-11 text-[#1d4ed8]" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                            <path d="<?= $escaper->escapeHtmlAttr($footerBenefit[2]) ?>" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <div class="text-sm font-black text-slate-950"><?= $escaper->escapeHtml($footerBenefit[0]) ?></div>
                        <div class="mt-1 text-xs text-slate-600"><?= $escaper->escapeHtml($footerBenefit[1]) ?></div>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
    </section>
</main>

<script>
require(['jquery'], function ($) {
    var $mainImage = $('#jersey-pdp-main-image');
    var $thumbs = $('.js-pdp-thumb');

    $thumbs.on('click', function () {
        var $thumb = $(this);
        var imageUrl = $thumb.data('full-image');

        if (!imageUrl || !$mainImage.length) {
            return;
        }

        $mainImage.attr('src', imageUrl);
        $thumbs.removeClass('border-[#1d4ed8] ring-4 ring-blue-100').addClass('border-slate-200');
        $thumb.removeClass('border-slate-200').addClass('border-[#1d4ed8] ring-4 ring-blue-100');
    });

    $('.js-qty-minus, .js-qty-plus').on('click', function () {
        var $qty = $('#qty');
        var currentValue = parseFloat($qty.val()) || 1;
        var nextValue = $(this).hasClass('js-qty-plus') ? currentValue + 1 : Math.max(1, currentValue - 1);

        $qty.val(nextValue).trigger('change');
    });

    $('.js-pdp-tab').on('click', function () {
        var $tab = $(this);
        var target = $tab.data('tab-target');

        if (!target || !$('#' + target).length) {
            return;
        }

        $('.js-pdp-tab')
            .removeClass('border-[#1d4ed8] bg-[#1d4ed8] text-white shadow-sm')
            .addClass('border-transparent text-slate-500')
            .attr('aria-selected', 'false');

        $tab
            .removeClass('border-transparent text-slate-500')
            .addClass('border-[#1d4ed8] bg-[#1d4ed8] text-white shadow-sm')
            .attr('aria-selected', 'true');

        $('.js-pdp-tab-panel').addClass('hidden');
        $('#' + target).removeClass('hidden');
    });
});
</script>

<script type="text/x-magento-init">
{
    "#product_addtocart_form": {
        "Magento_Catalog/js/validate-product": {}
    },
    "[data-role=priceBox][data-price-box=product-id-<?= $escaper->escapeHtml($_product->getId()) ?>]": {
        "priceBox": {
            "priceConfig": <?= /* @noEscape */ $block->getJsonConfig() ?>
        }
    }
}
</script>
<?php endif; ?>
