<?php
/** @var \Magento\Framework\View\Element\Template $block */
/** @var \Magento\Framework\Escaper $escaper */

$navItems = [
    ['label' => __('Home'), 'href' => $block->getUrl('')],
    ['label' => __('About Us'), 'href' => $block->getUrl('about-us')],
    ['label' => __('Contact Page'), 'href' => $block->getUrl('contact-page')],
    ['label' => __('Category Page'), 'href' => $block->getUrl('category-page')],
];
?>

<header
    x-data="{ mobileMenuOpen: false }"
    @keydown.escape.window="mobileMenuOpen = false"
    class="header-section sticky top-0 z-[999] bg-white/95 text-[#101114] shadow-sm backdrop-blur-xl"
>
    <!-- tvoj top bar i desktop/mobile header ostaju isti -->

    <button
        type="button"
        @click.prevent.stop="mobileMenuOpen = !mobileMenuOpen"
        class="relative z-[1001] grid h-11 w-11 place-items-center rounded-full bg-[#f4f4f5] lg:hidden"
    >
        ☰
    </button>

    <div
        x-cloak
        x-show="mobileMenuOpen"
        @click="mobileMenuOpen = false"
        class="fixed inset-0 z-[998] bg-black/50 lg:hidden"
    ></div>

    <div
        x-cloak
        x-show="mobileMenuOpen"
        x-transition:enter="transition ease-out duration-300"
        x-transition:enter-start="-translate-x-full"
        x-transition:enter-end="translate-x-0"
        x-transition:leave="transition ease-in duration-200"
        x-transition:leave-start="translate-x-0"
        x-transition:leave-end="-translate-x-full"
        class="fixed left-0 top-0 z-[1000] h-screen w-[320px] bg-white shadow-2xl lg:hidden"
    >
        <div class="flex h-[72px] items-center justify-between border-b border-black/5 px-5">
            <div>
                <span class="text-[22px] font-black uppercase">Jersey</span>
                <span class="text-[13px] font-black uppercase text-[#e31b23]">Store</span>
            </div>

            <button
                type="button"
                @click.prevent.stop="mobileMenuOpen = false"
                class="grid h-10 w-10 place-items-center rounded-full bg-[#f4f4f5]"
            >
                ✕
            </button>
        </div>

        <nav class="grid px-5 py-4 text-sm font-black uppercase">
            <?php foreach ($navItems as $item): ?>
                <a
                    href="<?= $escaper->escapeUrl($item['href']) ?>"
                    @click="mobileMenuOpen = false"
                    class="border-b border-black/5 py-5 text-[#101114]"
                >
                    <?= $escaper->escapeHtml($item['label']) ?>
                </a>
            <?php endforeach; ?>
        </nav>
    </div>
</header>