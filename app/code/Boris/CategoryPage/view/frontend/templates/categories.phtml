<?php
/** @var \Boris\CategoryPage\Block\Categories $block */
/** @var \Magento\Framework\Escaper $escaper */

$categories = $block->getCategories();
?>

<section class="relative left-1/2 w-screen -translate-x-1/2 overflow-hidden bg-slate-950 text-white">
    <div class="absolute inset-0">
        <div class="absolute left-1/2 top-10 h-80 w-80 -translate-x-1/2 rounded-full bg-yellow-300/20 blur-3xl"></div>
        <div class="absolute right-8 top-32 h-72 w-72 rounded-full bg-blue-500/10 blur-3xl"></div>
    </div>

    <div class="relative mx-auto max-w-7xl px-6 py-16 lg:px-8 lg:py-20">
        <p class="inline-flex rounded-full border border-yellow-300/30 bg-yellow-300/10 px-4 py-2 text-xs font-black uppercase tracking-[0.22em] text-yellow-300">
            <?= $escaper->escapeHtml(__('JerseyStore Categories')) ?>
        </p>
        <h1 class="mt-5 max-w-4xl text-5xl font-black leading-none tracking-tight sm:text-6xl lg:text-7xl">
            <?= $escaper->escapeHtml(__('Shop by category')) ?>
        </h1>
        <p class="mt-6 max-w-2xl text-lg leading-8 text-slate-300">
            <?= $escaper->escapeHtml(__('All active categories from the Magento admin panel are shown here automatically.')) ?>
        </p>
    </div>
</section>

<section class="relative left-1/2 w-screen -translate-x-1/2 bg-slate-100 text-slate-950">
    <div class="mx-auto max-w-7xl px-6 py-12 lg:px-8 lg:py-16">
        <?php if (!$categories): ?>
            <div class="rounded-lg border border-slate-200 bg-white p-10 text-center shadow-sm">
                <p class="text-lg font-black">
                    <?= $escaper->escapeHtml(__('No active storefront categories found.')) ?>
                </p>
                <p class="mt-3 text-sm leading-6 text-slate-500">
                    <?= $escaper->escapeHtml(__('Create categories under the active store root category, enable them, and set Include in Menu to Yes.')) ?>
                </p>
            </div>
        <?php else: ?>
            <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
                <?php foreach ($categories as $category): ?>
                    <a
                        href="<?= $escaper->escapeUrl($block->getCleanCategoryUrl($category)) ?>"
                        class="group rounded-lg border border-slate-200 bg-white p-7 shadow-sm transition duration-300 hover:-translate-y-1 hover:border-yellow-300 hover:shadow-xl hover:shadow-slate-950/10"
                    >
                        <span class="inline-flex rounded-full bg-yellow-300 px-3 py-1 text-xs font-black uppercase tracking-[0.16em] text-slate-950">
                            <?= $escaper->escapeHtml(__('Category')) ?>
                        </span>
                        <h2 class="mt-5 text-2xl font-black tracking-tight text-slate-950">
                            <?= $escaper->escapeHtml($category->getName()) ?>
                        </h2>
                        <p class="mt-3 text-sm leading-6 text-slate-500">
                            <?= $escaper->escapeHtml($block->getCategoryDescription($category)) ?>
                        </p>
                        <span class="mt-6 inline-flex text-sm font-black text-yellow-700 transition duration-300 group-hover:translate-x-1">
                            <?= $escaper->escapeHtml(__('Open category')) ?> &rarr;
                        </span>
                    </a>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>
    </div>
</section>
